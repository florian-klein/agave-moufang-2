diff --git a/ipc-streamer/client/src/main.rs b/ipc-streamer/client/src/main.rs
index 4e79c967d..b0bdb6daf 100644
--- a/ipc-streamer/client/src/main.rs
+++ b/ipc-streamer/client/src/main.rs
@@ -1,8 +1,6 @@
 use clap::Parser;
 use shm_accounts_shared::ShmReader;
 use solana_pubkey::Pubkey;
-use std::str::FromStr;
-use std::time::Instant;
 
 /// Streams pubkey+data updates from the shared ring (SPSC).
 #[derive(Parser, Debug)]
@@ -25,24 +23,22 @@ fn main() -> anyhow::Result<()> {
     let args = Args::parse();
     let mut r = ShmReader::open(&args.path)?;
 
-use std::time::{SystemTime, UNIX_EPOCH};
+    use std::time::{SystemTime, UNIX_EPOCH};
 
-loop {
-    if let Some((pubkey, data)) = r.poll_next() {
-        let now = SystemTime::now()
-            .duration_since(UNIX_EPOCH)
-            .unwrap();
+    loop {
+        if let Some((pubkey, lamports, _data)) = r.poll_next() {
+            let now = SystemTime::now().duration_since(UNIX_EPOCH).unwrap();
 
-        println!(
-            "[{:?}] pubkey: {:?}",
-            now,
-            Pubkey::from(pubkey)
-        );
-        continue;
-    }
+            println!(
+                "[{:?}] pubkey: {:?}, lamports: {}",
+                now,
+                Pubkey::from(pubkey),
+                lamports
+            );
+            continue;
+        }
 
-    std::hint::spin_loop();
-    std::thread::sleep(std::time::Duration::from_micros(50));
-}
-    Ok(())
+        std::hint::spin_loop();
+        std::thread::sleep(std::time::Duration::from_micros(50));
+    }
 }
diff --git a/ipc-streamer/plugin/src/lib.rs b/ipc-streamer/plugin/src/lib.rs
index 08116cd54..0098d07db 100644
--- a/ipc-streamer/plugin/src/lib.rs
+++ b/ipc-streamer/plugin/src/lib.rs
@@ -2,20 +2,20 @@ use agave_geyser_plugin_interface::geyser_plugin_interface::{
     GeyserPlugin, GeyserPluginError, ReplicaAccountInfo, ReplicaAccountInfoV2,
     ReplicaAccountInfoV3, ReplicaAccountInfoVersions, Result as GeyserResult,
 };
+use arc_swap::ArcSwap;
+use dashmap::DashSet;
 use log::info;
+use shm_accounts_shared::ShmWriter;
 use solana_pubkey::Pubkey;
+use std::ffi::CStr;
+use std::io::Read;
 use std::net::{TcpListener, TcpStream};
-use std::sync::{Arc, Mutex};
-use std::thread;
+use std::path::Path;
 use std::str::FromStr;
-use arc_swap::ArcSwap;
-use std::io::Read;
 use std::sync::atomic::AtomicBool;
 use std::sync::atomic::Ordering;
-use shm_accounts_shared::ShmWriter;
-use std::ffi::CStr;
-use std::path::Path;
-use dashmap::DashSet;
+use std::sync::{Arc, Mutex};
+use std::thread;
 
 #[derive(Default)]
 pub struct ShmPlugin {
@@ -80,10 +80,7 @@ impl GeyserPlugin for ShmPlugin {
                 .name(format!("geyser-filter-{}", port))
                 .spawn(move || run_filter_server(port, filter))
                 .map_err(|e| GeyserPluginError::Custom(Box::new(e)))?;
-            info!(
-                "filter server listening on 127.0.0.1:{}",
-                port
-            );
+            info!("filter server listening on 127.0.0.1:{}", port);
         } else {
             info!("filter server disabled (no filter_port) — allowing all pubkeys");
         }
@@ -106,22 +103,30 @@ impl GeyserPlugin for ShmPlugin {
         _is_startup: bool,
     ) -> GeyserResult<()> {
         // Fast path: read a snapshot of the filter without locking
-        let (pubkey_bytes, data): (&[u8], &[u8]) = match account {
-            ReplicaAccountInfoVersions::V0_0_1(ReplicaAccountInfo { pubkey, data, .. }) => {
-                (pubkey, data)
-            }
-            ReplicaAccountInfoVersions::V0_0_2(ReplicaAccountInfoV2 { pubkey, data, .. }) => {
-                (pubkey, data)
-            }
-            ReplicaAccountInfoVersions::V0_0_3(ReplicaAccountInfoV3 { pubkey, data, .. }) => {
-                (pubkey, data)
-            }
+        let (pubkey_bytes, lamports, data): (&[u8], u64, &[u8]) = match account {
+            ReplicaAccountInfoVersions::V0_0_1(ReplicaAccountInfo {
+                pubkey,
+                data,
+                lamports,
+                ..
+            }) => (pubkey, *lamports, data),
+            ReplicaAccountInfoVersions::V0_0_2(ReplicaAccountInfoV2 {
+                pubkey,
+                data,
+                lamports,
+                ..
+            }) => (pubkey, *lamports, data),
+            ReplicaAccountInfoVersions::V0_0_3(ReplicaAccountInfoV3 {
+                pubkey,
+                data,
+                lamports,
+                ..
+            }) => (pubkey, *lamports, data),
         };
         if data.is_empty() {
             return Ok(());
         }
 
-
         let mut pk = [0u8; 32];
         pk.copy_from_slice(pubkey_bytes);
 
@@ -130,88 +135,82 @@ impl GeyserPlugin for ShmPlugin {
         }
 
         if let Some(w) = self.writer.lock().unwrap().as_mut() {
-            let _ = w.push(&pk, data);
+            let _ = w.push(&pk, data, lamports);
         }
         Ok(())
     }
 }
 
-fn run_filter_server(
-        port: u16,
-        filter: Arc<DashSet<[u8; 32]>>,
-    ) {
-        let listener = TcpListener::bind(("127.0.0.1", port))
-            .expect("bind 127.0.0.1:port for filter server");
-        for conn in listener.incoming() {
-            match conn {
-                Ok(mut stream) => {
-                    if let Err(e) = handle_conn(&mut stream, &filter) {
-                        let _ = writeln_safely(&mut stream, &format!("ERR {e}\n"));
-                    }
+fn run_filter_server(port: u16, filter: Arc<DashSet<[u8; 32]>>) {
+    let listener =
+        TcpListener::bind(("127.0.0.1", port)).expect("bind 127.0.0.1:port for filter server");
+    for conn in listener.incoming() {
+        match conn {
+            Ok(mut stream) => {
+                if let Err(e) = handle_conn(&mut stream, &filter) {
+                    let _ = writeln_safely(&mut stream, &format!("ERR {e}\n"));
                 }
-                Err(_) => continue,
             }
+            Err(_) => continue,
         }
     }
+}
 
-    fn handle_conn(
-        stream: &mut TcpStream,
-        filter: &Arc<DashSet<[u8; 32]>>,
-    ) -> std::io::Result<()> {
-        let mut buf = Vec::with_capacity(64 * 1024);
-        stream.read_to_end(&mut buf)?;
-        let body = String::from_utf8_lossy(&buf);
-
-        // Accept JSON array or whitespace/newline-separated keys
-        let res = if body.trim_start().starts_with('[') {
-            parse_json_array(&body, &filter)
-        } else {
-            parse_whitespace_list(&body, &filter)
-        };
-
-        match res {
-            Ok(_) => {
-                let cnt = filter.len();
-                writeln_safely(stream, &format!("OK {cnt}\n"))?;
-            }
-            Err(e) => {
-                writeln_safely(stream, &format!("ERR {e}\n"))?;
-            }
+fn handle_conn(stream: &mut TcpStream, filter: &Arc<DashSet<[u8; 32]>>) -> std::io::Result<()> {
+    let mut buf = Vec::with_capacity(64 * 1024);
+    stream.read_to_end(&mut buf)?;
+    let body = String::from_utf8_lossy(&buf);
+
+    // Accept JSON array or whitespace/newline-separated keys
+    let res = if body.trim_start().starts_with('[') {
+        parse_json_array(&body, &filter)
+    } else {
+        parse_whitespace_list(&body, &filter)
+    };
+
+    match res {
+        Ok(_) => {
+            let cnt = filter.len();
+            writeln_safely(stream, &format!("OK {cnt}\n"))?;
+        }
+        Err(e) => {
+            writeln_safely(stream, &format!("ERR {e}\n"))?;
         }
-        Ok(())
     }
+    Ok(())
+}
 
-    fn writeln_safely(stream: &mut TcpStream, s: &str) -> std::io::Result<()> {
-        use std::io::Write;
-        let _ = stream.write_all(s.as_bytes());
-        Ok(())
-    }
+fn writeln_safely(stream: &mut TcpStream, s: &str) -> std::io::Result<()> {
+    use std::io::Write;
+    let _ = stream.write_all(s.as_bytes());
+    Ok(())
+}
 
-    fn parse_json_array(body: &str, filter: &Arc<DashSet<[u8; 32]>>) -> Result<(), String>{
-        let vals: Vec<String> = serde_json::from_str(body)
-            .map_err(|e| format!("invalid JSON array: {e}"))?;
-        parse_keys(vals.into_iter(), filter)
-    }
+fn parse_json_array(body: &str, filter: &Arc<DashSet<[u8; 32]>>) -> Result<(), String> {
+    let vals: Vec<String> =
+        serde_json::from_str(body).map_err(|e| format!("invalid JSON array: {e}"))?;
+    parse_keys(vals.into_iter(), filter)
+}
 
-    fn parse_whitespace_list(body: &str, filter: &Arc<DashSet<[u8; 32]>>) -> Result<(), String>{
-        let vals = body.split_whitespace().map(|s| s.to_string());
-        parse_keys(vals, filter)
-    }
+fn parse_whitespace_list(body: &str, filter: &Arc<DashSet<[u8; 32]>>) -> Result<(), String> {
+    let vals = body.split_whitespace().map(|s| s.to_string());
+    parse_keys(vals, filter)
+}
 
-    fn parse_keys<I>(iter: I, filter: &Arc<DashSet<[u8; 32]>>) -> Result<(), String>
-    where
-        I: IntoIterator<Item = String>,
-    {
-        filter.clear();
-        for s in iter {
-            if s.is_empty() {
-                continue;
-            }
-            let pk = Pubkey::from_str(&s).map_err(|e| format!("bad pubkey '{s}': {e}"))?;
-            filter.insert(*pk.as_array()); // Pubkey derefs to [u8;32]
+fn parse_keys<I>(iter: I, filter: &Arc<DashSet<[u8; 32]>>) -> Result<(), String>
+where
+    I: IntoIterator<Item = String>,
+{
+    filter.clear();
+    for s in iter {
+        if s.is_empty() {
+            continue;
         }
-        Ok(())
+        let pk = Pubkey::from_str(&s).map_err(|e| format!("bad pubkey '{s}': {e}"))?;
+        filter.insert(*pk.as_array()); // Pubkey derefs to [u8;32]
     }
+    Ok(())
+}
 
 // Dynamic entry point
 #[unsafe(no_mangle)]
diff --git a/ipc-streamer/plugin/src/main.rs b/ipc-streamer/plugin/src/main.rs
deleted file mode 100644
index e7a11a969..000000000
--- a/ipc-streamer/plugin/src/main.rs
+++ /dev/null
@@ -1,3 +0,0 @@
-fn main() {
-    println!("Hello, world!");
-}
diff --git a/ipc-streamer/shared/src/lib.rs b/ipc-streamer/shared/src/lib.rs
index 9527a7f4c..703880cd9 100644
--- a/ipc-streamer/shared/src/lib.rs
+++ b/ipc-streamer/shared/src/lib.rs
@@ -1,7 +1,7 @@
 use bytemuck::{Pod, Zeroable};
+use libc::eventfd;
 use libc::EFD_CLOEXEC;
 use libc::EFD_NONBLOCK;
-use libc::eventfd;
 use memmap2::{MmapMut, MmapOptions};
 use nix::fcntl::OFlag;
 use nix::sys::mman::shm_open;
@@ -45,6 +45,7 @@ pub struct RecHeader {
     pub data_len: u32,    // account data length in bytes
     pub seqno: u64,       // strictly increasing
     pub pubkey: [u8; 32], // raw pubkey
+    pub lamports: u64,    // lamports
 }
 
 impl RecHeader {
@@ -153,7 +154,12 @@ impl ShmWriter {
     }
 
     /// Push one record (pubkey + data). Returns (seqno, write_off) on success.
-    pub fn push(&mut self, pubkey: &[u8; 32], data: &[u8]) -> Result<(u64, usize), ShmError> {
+    pub fn push(
+        &mut self,
+        pubkey: &[u8; 32],
+        data: &[u8],
+        lamports: u64,
+    ) -> Result<(u64, usize), ShmError> {
         let hdr = unsafe { &mut *self.hdr };
         let mut w = hdr.write_off as usize;
         let r = hdr.read_off as usize; // consumer-owned; we don't mutate it
@@ -175,6 +181,7 @@ impl ShmWriter {
                     data_len: 0,
                     seqno: 0,
                     pubkey: [0; 32],
+                    lamports,
                 };
                 unsafe {
                     std::ptr::copy_nonoverlapping(
@@ -199,6 +206,7 @@ impl ShmWriter {
             data_len: data.len() as u32,
             seqno: seq,
             pubkey: *pubkey,
+            lamports,
         };
 
         // 1) Copy payload
@@ -279,7 +287,7 @@ impl ShmReader {
     }
 
     /// Tries to read one record; returns (pubkey, data) as slices into the mmap or scratch copy.
-    pub fn poll_next<'a>(&'a mut self) -> Option<([u8; 32], &'a [u8])> {
+    pub fn poll_next<'a>(&'a mut self) -> Option<([u8; 32], u64, &'a [u8])> {
         let hdr = unsafe { &mut *self.hdr };
         let mut r = hdr.read_off as usize;
         let w = hdr.write_off as usize;
@@ -334,7 +342,7 @@ impl ShmReader {
             let data =
                 unsafe { std::slice::from_raw_parts(self.buf.add(data_off), rh.data_len as usize) };
             hdr.read_off = rec_end as u64;
-            return Some((rh.pubkey, data));
+            return Some((rh.pubkey, rh.lamports, data));
         } else {
             // wrapped payload; copy into scratch
             let first = self.cap - rec_start - RecHeader::size();
@@ -353,7 +361,7 @@ impl ShmReader {
                 );
             }
             hdr.read_off = (rec_end - self.cap) as u64; // wrapped
-            return Some((rh.pubkey, &self.scratch));
+            return Some((rh.pubkey, rh.lamports, &self.scratch));
         }
     }
 }
